#include "main.h"
/**
*positive_or_negative - checks the state of the number
*@i: the number being checked
*Return: 0 (pass)
*/
void positive_or_negative(int i)
{
if (i < 0)
printf("%d is positive", i);
else
printf("%d is positive", i);
}
